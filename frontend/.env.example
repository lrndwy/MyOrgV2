# Browser uses same-origin proxy /api/backend by default (see next.config.ts).
# Only set this if you want the browser to call the API directly (cross-origin).
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8080

# Server-side / rewrite target
API_INTERNAL_URL=http://127.0.0.1:8080
